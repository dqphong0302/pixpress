{
  "name": "pixpress",
  "version": "1.0.0",
  "description": "PixPress - Nén, chuyển định dạng, resize (Facebook HD 2048) và cắt ảnh. 100% client-side, triển khai trên Cloudflare Workers.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && wrangler deploy"
  },
  "devDependencies": {
    "vite": "^8.2.2",
    "wrangler": "^4.127.1"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^1.0.1",
    "exifr": "^7.1.3",
    "heic-to": "^1.5.2",
    "jszip": "^3.10.1"
  }
}
